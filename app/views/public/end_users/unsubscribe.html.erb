<div>
  <h2>本当に退会しますか？</h2>
  <div>
    <p>退会する場合は、『退会する』をクリックしてください。</p>
  </div>
  <div>
    <%= link_to '退会しない', end_users_path(@public) %>
    <%= link_to end_users_path, method: :patch do %>
	  	退会する
		<% end %>
  </div>
</div>